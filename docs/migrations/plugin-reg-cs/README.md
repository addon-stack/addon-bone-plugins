# `@adnbn/plugin-reg-cs` history import

The source repository was filtered into `packages/@adnbn/plugin-reg-cs` with `git-filter-repo` 2.47.0.

- Source `develop`: `765ecef673b936bb306bceb87f8411ffd21de4a7`
- Source `main`: `bba524352b85e6dbb8f6961f274d7cc908b9ea90`
- Imported commits: 73
- Imported annotated release tags: 11
- Tag mapping: `vX.Y.Z` to `@adnbn/plugin-reg-cs@X.Y.Z`

`commit-map.txt` records each original commit ID and its rewritten monorepo commit ID. `ref-map.txt` records source
and imported object IDs and ref names for both heads and annotated tags. The archived source repository remains the authority for GitHub-only metadata such as pull-request
comments, issue discussions, release assets, and Actions logs.

The two `refs/remotes/plugin-reg-cs-import/*` names identify the local refs used during the import. They do not need to
be pushed: both rewritten branch tips and their ancestors are reachable from the monorepo `main` merge commit. The
scoped annotated tag refs are preserved and must be pushed with `main`.
